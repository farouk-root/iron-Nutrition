{% extends 'base.html.twig' %}

{% block title %}Cabinet{% endblock %}

{% block body %}
    <h1>Cabinet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cabinet.id }}</td>
            </tr>
            <tr>
                <th>NomMED</th>
                <td>{{ cabinet.nomMED }}</td>
            </tr>
            <tr>
                <th>PrenomMED</th>
                <td>{{ cabinet.prenomMED }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ cabinet.specialite }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cabinet.adresse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cabinet.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ cabinet.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cabinet_index') }}">back to list</a>

    <a href="{{ path('app_cabinet_edit', {'id': cabinet.id}) }}">edit</a>

    {{ include('cabinet/_delete_form.html.twig') }}
{% endblock %}
