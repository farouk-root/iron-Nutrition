{% extends 'basedb.html.twig' %}

{% block title %}New Cabinet{% endblock %}

{% block body %}
<section class="section">
    <h1>Create new Cabinet</h1>

    {{ include('cabinet/_form.html.twig') }}

    <a href="{{ path('app_cabinet_index') }}">back to list</a>
</section>
{% endblock %}
